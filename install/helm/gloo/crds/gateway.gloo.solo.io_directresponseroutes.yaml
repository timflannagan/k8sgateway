---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app: gloo-gateway
    app.kubernetes.io/name: gloo-gateway
  name: directresponseroutes.gateway.gloo.solo.io
spec:
  group: gateway.gloo.solo.io
  names:
    categories:
    - gloo-gateway
    kind: DirectResponseRoute
    listKind: DirectResponseRouteList
    plural: directresponseroutes
    shortNames:
    - drr
    singular: directresponseroute
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              body:
                type: string
              code:
                format: int32
                maximum: 599
                minimum: 100
                type: integer
            required:
            - code
            type: object
            x-kubernetes-validations:
            - message: The 'body' field is required when 'code' is a 2xx status code
              rule: self.code < 200 || self.code >= 300 || has(self.body)
          status:
            properties:
              lastUpdated:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
